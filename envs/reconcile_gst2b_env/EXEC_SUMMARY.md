1. OpenEnv environment for Indian GST Input-Tax-Credit reconciliation against the monthly GSTR-2B return.
2. Regulated ground truth, 5 planted mismatch types, and directed 3-cycle circular-trading rings detectable by `networkx.simple_cycles`.
3. 4-component arithmetic reward, every component clamped to `[0.01, 0.99]`; 6 adversarial attacks all score <0.45, CI-enforced.
4. Tier 1+2 GRPO fixes diagnosed + validated on 10-step T4 smoke (`data/smoke_test_10step.json`); plateau at 0.6B scale; 3B + A100 × 4 h is next compute ask.
5. Trained Qwen3-4B SFT + P3 GRPO on A100 SXM4-80GB (Day 1 on-site, 2026-04-25): n=5 mean composite reward **0.305** (post-GRPO with Tier 2c length-shaping bonus). SFT-only baseline (pre-GRPO) was 0.280; P3 GRPO lift +0.025. Prompted Qwen2.5-3B baseline (held as comparison anchor): 0.18, delta 1.18 over raw, CI95 [1.09, 1.27]. Trained-vs-prompted lift: +0.125. Trained number sits between prompted baseline 0.18 and red-team ceiling 0.45.
6. Reward has gradient: 81 distinct totals, σ = 0.50, 100% done-rate across 100 random-policy episodes (2 modes).
7. Differentiation: not Gaia2 / AppWorld / τ-bench (general tool-use), tests adversarial domain reasoning against hidden ground truth.
8. Status: environment shippable (42 tests green, `openenv validate --verbose` passes); on-site Day 1 trained SFT + P3 GRPO with length-shaping mitigation land real numbers (n=5 mean 0.305); FM5 reward-landscape inversion partially mitigated, full bimodal flip is post-hackathon agenda (LESSONS_LEARNED §1 P3 GRPO sub-block).
9. Sharpest self-attack: bimodal policy persists post-P3 GRPO. Mode A marking trajectories now lift R1+R2 off the 0.01 floor (R1≈0.15, R2≈0.41 on the marking seeds), but 2/5 seeds remain in the Mode B query_only attack shape. Stronger length-shaping or class-balanced SFT subsampling is the post-hackathon agenda to flip the full distribution.
10. Five distinct failure modes documented across pre-onsite + on-site. Pre-onsite Kaggle: 0.6B pins R1/R2 (bimodal tool-call rate); 1.7B pins all R's (entropy collapse to 0.12); 4B pins R4 (over-query budget-exhaustion). On-site A100 (Day 1): Failure Mode 4 (audit-OOD trap chain, generalizes across SFT/GRPO/eval/audit code paths, empirically confirmed by the buggy eval callback reporting flat 0.354 across all 100 GRPO steps); Failure Mode 5 (reward-landscape inversion, query_only attack at 0.353 outscores marking trajectory at max 0.26, partially mitigated by P3 Tier 2c length-shaping bonus that lifted n=5 mean from 0.280 to 0.305 without flipping the bimodal distribution; post-hackathon agenda is stronger shaping or class-balanced SFT). Post-SFT + P3 GRPO n=5 mean composite reward **0.305** ships as the trained number. Full chain in `LESSONS_LEARNED.md` §1.
11. 🎥 **90-second demo**: https://www.youtube.com/watch?v=K-sZ8c1TMjw, ring viewer, reward breakdown, triple-failure-mode evidence.
12. Stack: OpenEnv (env + FastAPI server) + TRL (GRPO trainer, `environment_factory` pattern) + PEFT (LoRA rank 16, q/k/v/o targets). Unsloth drop-in ready for Phase 2 SFT / Phase 3 GRPO efficiency if on-site compute is tight; RLVR-style reward (verifier-based, no learned reward model) makes on-site scaling predictable.
