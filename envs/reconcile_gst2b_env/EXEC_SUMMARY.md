1. OpenEnv environment for Indian GST Input-Tax-Credit reconciliation against the monthly GSTR-2B return.
2. Regulated ground truth, 5 planted mismatch types, and directed 3-cycle circular-trading rings detectable by `networkx.simple_cycles`.
3. 4-component arithmetic reward, every component clamped to `[0.01, 0.99]`; 6 adversarial attacks all score <0.45, CI-enforced.
4. Tier 1+2 GRPO fixes diagnosed + validated on 10-step T4 smoke (`data/smoke_test_10step.json`); plateau at 0.6B scale; 3B + A100 × 4 h is next compute ask.
5. Trained Qwen3-4B SFT on A100 SXM4-80GB (Day 1 on-site, 2026-04-25): n=5 mean composite reward **0.280**. Prompted Qwen2.5-3B baseline (held as comparison anchor): 0.18, delta 1.18 over raw, CI95 [1.09, 1.27]. Trained-vs-prompted lift: +0.10. Trained number sits between prompted baseline 0.18 and red-team ceiling 0.45.
6. Reward has gradient: 81 distinct totals, σ = 0.50, 100% done-rate across 100 random-policy episodes (2 modes).
7. Differentiation: not Gaia2 / AppWorld / τ-bench (general tool-use), tests adversarial domain reasoning against hidden ground truth.
8. Status: environment shippable (42 tests green, `openenv validate --verbose` passes); on-site Day 1 trained SFT lands real numbers; GRPO Phase 3 deferred per reward-landscape analysis (LESSONS_LEARNED §1 Failure Mode 5).
9. Sharpest self-attack: R1 and R2 stay at floor (~0.01), prompting fixes surface behavior but not reconciliation reasoning. That's the compute ask on line 10: real training to lift R1/R2.
10. Five distinct failure modes documented across pre-onsite + on-site. Pre-onsite Kaggle: 0.6B pins R1/R2 (bimodal tool-call rate); 1.7B pins all R's (entropy collapse to 0.12); 4B pins R4 (over-query budget-exhaustion). On-site A100 (Day 1): Failure Mode 4 (audit-script OOD trap chain, 4 sequential audit/eval bugs that fabricated false collapse signatures before the real model behavior could be observed); Failure Mode 5 (reward-landscape inversion at Phase 2 → Phase 3 boundary, where query_only attack at 0.353 outscores marking trajectory at max 0.26, blocking GRPO progression without modifying frozen `rewards.py`). Post-SFT n=5 mean composite reward **0.280** ships as the trained number; GRPO Phase 3 deferred to post-hackathon validation. Full chain in `LESSONS_LEARNED.md` §1.
11. 🎥 **90-second demo**: https://www.youtube.com/watch?v=rglR1hGgdb8, ring viewer, reward breakdown, triple-failure-mode evidence.
12. Stack: OpenEnv (env + FastAPI server) + TRL (GRPO trainer, `environment_factory` pattern) + PEFT (LoRA rank 16, q/k/v/o targets). Unsloth drop-in ready for Phase 2 SFT / Phase 3 GRPO efficiency if on-site compute is tight; RLVR-style reward (verifier-based, no learned reward model) makes on-site scaling predictable.
